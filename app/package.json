{
  "name": "devops-challenge-api",
  "version": "1.0.0",
  "description": "Sample application for DevOps challenge — containerized and deployed to AWS ECS Fargate",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --forceExit",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "express": "4.21.1"
  },
  "devDependencies": {
    "jest": "29.7.0",
    "supertest": "7.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}